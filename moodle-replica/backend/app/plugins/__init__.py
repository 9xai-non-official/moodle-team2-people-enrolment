"""Plugin packages live here — one sub-package per plugin, each exposing
MANIFEST + HANDLERS (see services/plugin_core.py). The framework itself stays
in app/services so registry discovery never imports itself."""
