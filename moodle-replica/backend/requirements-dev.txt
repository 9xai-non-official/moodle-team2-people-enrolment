# Test-only deps (kept separate from the app's runtime requirements.txt).
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.24   # only needed for the DB-gated integration tests
