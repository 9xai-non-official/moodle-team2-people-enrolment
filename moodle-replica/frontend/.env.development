# Dev defaults (committed). Personal overrides go in .env.local (gitignored).
# Mock mode on while backend endpoints are stubs — header shows MOCK DATA.
# Flip to 0 (or set in .env.local) as real endpoints land; unmatched routes
# already fall through to the real backend either way. NO MOCKS ON THURSDAY.
VITE_USE_MOCKS=1
