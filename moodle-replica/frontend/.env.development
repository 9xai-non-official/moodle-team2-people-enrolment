# Dev defaults (committed). The backend is now real and its DATABASE_URL ships
# with the repo, so real mode is the default: pull + run and you're on the DB.
# To demo offline against mocks, set VITE_USE_MOCKS=1 in .env.development.local
# (gitignored) — that file OUTRANKS this one; plain .env.local does NOT.
VITE_USE_MOCKS=0
VITE_API_URL=http://localhost:8010
