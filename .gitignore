__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
build/
.env
.env.local
.env.*.local
.DS_Store
*.log
.gstack/

# Secrets must never be committed. Real values live in a local .env (see .env.example)
# or are shared via a private channel — not in the repo.
TEAM_CREDENTIALS.md
secrets.*
*.key
.vercel/
.playwright-mcp/

# Claude Code scratch worktrees (per-session, never shared)
.claude/worktrees/
